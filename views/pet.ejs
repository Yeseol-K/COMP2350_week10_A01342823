<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Pets</title>
  </head>
  <body>
    <h1>All Pets</h1>
    <ul>
      <% pets.forEach(pet => { %>
      <li><%= pet.name %></li>
      <% }); %>
    </ul>
  </body>
</html>
